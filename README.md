# Yang Zhang personal website

This repository powers the GitHub Pages site at:

https://phyzhangyang.github.io/

## Local preview

Install the GitHub Pages dependencies once:

```sh
bundle install
```

Then serve the site locally:

```sh
bundle exec jekyll serve
```

Open the local URL printed by Jekyll, usually `http://127.0.0.1:4000/`.
